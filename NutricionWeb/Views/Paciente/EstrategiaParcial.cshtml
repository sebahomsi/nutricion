@model NutricionWeb.Models.Estrategia.EstrategiaViewModel

@using (Html.BeginForm("Create", "Estrategia"))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(x => x.Id)
    @Html.HiddenFor(x => x.PacienteId)

    @*<div class="form-horizontal">
            <div class="form-group">
                @Html.TextAreaFor(model => model.Descripcion, new { @class = "form-control", @style = "max-width:700px;width:600px;margin: 0 auto; margin-top:30px;" })
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary-custom" />
            </div>
        </div>*@

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.TextAreaFor(model => model.Descripcion, new { @class = "form-control", @style = "max-width:700px;width:600px; margin-top:30px;", @rows = "10" })
                @Html.ValidationMessageFor(model => model.Descripcion, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Guardar" class="btn btn-primary-custom" />
            </div>
        </div>
    </div>
}