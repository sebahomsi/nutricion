@using Antlr.Runtime.Tree
@model IEnumerable<NutricionWeb.Models.Mensaje.MensajeViewModel>

@{
    ViewBag.Title = "Mensajes Enviados";
}

<div class="titulo-ABM">
    <h4 class="detail-h4">Mensajes Enviados</h4>
</div>

<div class="row">
    <div class="col-lg-2" align="left">
        <div class="btn-group">
            @Html.ActionLink("Nuevo", "Create", new { }, new { @class = "btn btn-primary" })
        </div>
    </div>
    <div class="col-lg-2" align="left">
        <div class="btn-group">
            @Html.ActionLink("Nuevo", "Create","Mail", new { }, new { @class = "btn btn-primary" })
        </div>
    </div>
    @*<div class="col-lg-6" align="right">
        <form class="navbar-form navbar-right">
            @using (Html.BeginForm("IndexEnviados", "Mensaje", FormMethod.Get))
            {
                <form class="form-group-lg">
                    @Html.TextBox("CadenaBuscar", ViewBag.FilterValue as string, new { placeholder = "Ingrese una cadena...", @class = "form-control", autocomplete = "off" })
                    <button type="submit" class="btn btn-default">Buscar</button>
                </form>
            }
        </form>
    </div>*@
</div>
<br>
<div class="table-responsive">
<table class="table">
    <tr class="cabecera-tabla">

        <th>
            @Html.DisplayNameFor(model => model.EmailReceptor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Motivo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cuerpo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Visto)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.EmailReceptor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Motivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cuerpo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VistoStr)
            </td>
            <td align="right">
                @Html.ActionLink("Detalles", "Details", new { id = item.Id }, new { @class = "btn btn-default" })
            </td>
        </tr>
    }

</table>
    </div>
