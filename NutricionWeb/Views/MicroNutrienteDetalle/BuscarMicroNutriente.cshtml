@model IEnumerable<NutricionWeb.Models.MicroNutriente.MicroNutrienteViewModel>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model.OrderBy(model => model.Descripcion)) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        
        <td>
            <input type="button" id="seleccionar-micro" name="@item.Id" class="btn btn-primary-custom" value="Seleccionar" />
        </td>
    </tr>
}

</table>
